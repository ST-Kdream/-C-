#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <ctime>

using namespace std;

void record_save(bool& is_win, int& difficulty, int& attempts, int& max_num, int& chance, int& EP);
